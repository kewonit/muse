# Muse Frontend

This is the Next.js client for Muse.

## Development

```bash
copy .env.example .env.local
npm ci
npm run dev
```

The app expects `NEXT_PUBLIC_POCKETBASE_URL` to point at the Muse backend. See the root `README.md` for full local setup and release notes.
